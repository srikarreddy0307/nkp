apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: strip-prefixes
  namespace: default  # change this if your Ingress is in another namespace
spec:
  stripPrefix:
    prefixes:
      - /app1
      - /app2
